# ============================================
# PetSalon Docker 环境变量配置模板
# ============================================
#
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 修改 .env 中的值（特别是密码）
# 3. .env 文件不会被提交到 git（已在 .gitignore 中）
#
# ============================================

# --------------------------------------------
# SQL Server 配置
# --------------------------------------------

# SQL Server SA 账户密码
# 密码要求：至少 8 个字符，包含大小写字母、数字和特殊字符
# 示例：YourStrong!Passw0rd
SA_PASSWORD=YourStrong!Passw0rd

# 接受 Microsoft SQL Server 最终用户许可协议
# 可选值：Y（接受）/ N（拒绝）
ACCEPT_EULA=Y

# SQL Server 排序规则
# 繁体中文排序规则（区分大小写、区分声调）
# 其他选项：
#   - Chinese_Taiwan_Stroke_CI_AS（不区分大小写）
#   - SQL_Latin1_General_CP1_CI_AS（英文环境）
MSSQL_COLLATION=Chinese_Taiwan_Stroke_CS_AS

# SQL Server 端口映射
# 默认：1433（建议保持默认）
SQL_PORT=1433

# --------------------------------------------
# 数据卷配置（可选）
# --------------------------------------------

# 如果使用绑定挂载（Bind Mount）而非命名卷，需要设置以下路径
#
# Windows 用户示例：
# DATA_PATH=C:/Docker/PetSalon/sqlserver
#
# macOS 用户示例：
# DATA_PATH=/Users/YourName/Docker/PetSalon/sqlserver
#
# Linux 用户示例：
# DATA_PATH=/home/yourname/docker/petsalon/sqlserver
#
# 注意：Windows 路径请使用正斜杠 "/" 或双反斜杠 "\\"
# DATA_PATH=

# --------------------------------------------
# 应用程序配置（预留）
# --------------------------------------------

# 后端 API 端口（将来使用）
# API_PORT=5150

# 前端应用端口（将来使用）
# FRONTEND_PORT=3000

# --------------------------------------------
# 开发环境配置
# --------------------------------------------

# 环境标识（development / staging / production）
ENVIRONMENT=development

# 是否启用调试模式
# DEBUG=true
